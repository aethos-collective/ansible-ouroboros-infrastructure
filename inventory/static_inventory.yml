---
all:
  children:
    chromeos:
      hosts:
        chromebook:

    debian_family:
      hosts:
        paakaa-pi-0:
        paakaa-pi-1:
        paakaa-pi-2:
        google-cloud-free-tier-instance:
        oracle-cloud-free-tier-instance:
        dreamhost-shared-server:

    google_cloud:
      hosts:
        google-cloud-free-tier-instance: # always-free

    oracle_cloud:
      hosts:
        oracle-cloud-free-tier-instance: # always-free

    dreamhost_shared:
      hosts:
        dreamhost-shared-server:

    local:
      hosts:
        chromebook:
        paakaa-pi-0:
        paakaa-pi-1:
        paakaa-pi-2:

    remote_dedicated:
      children:
        google_cloud:
        oracle_cloud:

    remote_shared:
      children:
        dreamhost_shared:

    remote:
      children:
        remote_dedicated:
        remote_shared:

    raspberry_pi:
      hosts:
        paakaa-pi-0:
        paakaa-pi-1:

    atomic_pi:
      hosts:
        paakaa-pi-2:

    pi:
      children:
        raspberry_pi:
        atomic_pi:

    dedicated:
      children:
        pi:
        remote_dedicated:

    not_chromebook:
      children:
        pi:
        remote:

    arm64:
      hosts:
        paakaa-pi-0:

    arm32:
      hosts:
        paakaa-pi-1:

    amd64:
      hosts:
        chromebook:
        paakaa-pi-2:

    minecraft_bedrock_server:
      hosts:
        paakaa-pi-2:
