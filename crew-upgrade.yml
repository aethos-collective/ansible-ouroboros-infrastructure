---
- name: Upgrade Chromebrew packages on Chrome OS hosts.
  hosts: chromeos

  tasks:
    - name: Update and upgrade Chromebrew packages.
      ansible.builtin.shell:
        cmd: crew update
      # chromebrew: # TODO: create Chromebrew plugin based on Homebrew plugin
      #   upgrade_all: yes
      #   update_chromebrew: yes
