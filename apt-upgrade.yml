---
- name: Upgrade deb packages on all dedicated Debian-based hosts.
  hosts: dedicated:&debian_family

  tasks:
    - name: Update apt cache and upgrade apt packages.
      become: true
      apt:
        upgrade: yes
        update_cache: yes
        cache_valid_time: 86400 # one day
